<div align="center">

```
███████╗ ██████╗ ██████╗ ██╗  ██╗██╗███████╗██╗
╚════██║██╔═══██╗██╔══██╗██║  ██║██║██╔════╝██║
    ██╔╝██║   ██║██████╔╝███████║██║█████╗  ██║
   ██╔╝ ██║   ██║██╔═══╝ ██╔══██║██║██╔══╝  ██║
   ██║  ╚██████╔╝██║     ██║  ██║██║███████╗███████╗
   ╚═╝   ╚═════╝ ╚═╝     ╚═╝  ╚═╝╚═╝╚══════╝╚══════╝
```

**SOLIA · ZOPHIEL AI ENGINE**  
*Built by Aureon Software — Non-LLM Intelligence Architecture*

---

![Python](https://img.shields.io/badge/Python-3.11+-00d4ff?style=for-the-badge&logo=python&logoColor=white)
![Flask](https://img.shields.io/badge/Flask-REST_API-7c3aed?style=for-the-badge&logo=flask&logoColor=white)
![SQLite](https://img.shields.io/badge/SQLite-29741_docs-00ff88?style=for-the-badge&logo=sqlite&logoColor=black)
![Corpus](https://img.shields.io/badge/Corpus-55_domains-ff6b35?style=for-the-badge)
![Code_Engine](https://img.shields.io/badge/Code_Engine-105_patterns-00d4ff?style=for-the-badge)
![Deploy](https://img.shields.io/badge/Deploy-Railway-0f0f0f?style=for-the-badge&logo=railway&logoColor=white)
![Auth](https://img.shields.io/badge/Auth-API_Key_Bearer-ff2d55?style=for-the-badge)

</div>

---

## ◈ What Is Artificial Intelligence — Really?

Most people think AI is a magic black box. It is not. At its foundation, every AI system — including the large language models behind ChatGPT — is doing one thing:

> **Finding the most statistically likely pattern in data and returning it.**

That's it. No consciousness. No understanding. No soul.

Here is what modern AI actually consists of:

```
┌─────────────────────────────────────────────────────────────────────┐
│                      WHAT "AI" REALLY IS                            │
│                                                                     │
│   RAW DATA  →  PATTERN EXTRACTION  →  PROBABILITY  →  OUTPUT       │
│                                                                     │
│  Text / Images     Weights &          "What word is     Answer /   │
│  Audio / Code      Vectors in         most likely       Image /    │
│  Sensor data       high-dim space     next?" (LLM)      Action     │
│                                                                     │
│  ─────────────────────────────────────────────────────────────────  │
│                                                                     │
│  The "intelligence" is the compression of millions of examples      │
│  into a mathematical space where similar things cluster together.   │
│                                                                     │
│  Distance in that space = semantic similarity.                      │
│  Prediction in that space = the AI's "answer."                      │
└─────────────────────────────────────────────────────────────────────┘
```

### The Two Categories of AI

| Type | What It Does | Example | Limitation |
|------|-------------|---------|-----------|
| **Statistical / LLM** | Predicts the next token based on billions of training examples | GPT-4, Claude, Gemini | Hallucinates. No real reasoning. Just pattern completion. |
| **Symbolic / Rule-Based** | Follows explicit logical rules and equation chains | Zophiel / SOLIA | Only knows what you teach it, but never hallucinates. |

**Zophiel is built on the second category** — deterministic, corpus-driven, logic-chained intelligence. No neural weights. No hallucination. Every answer traces back to a real document or a real rule.

---

## ◈ The Asher Equation — How Technology Mirrors Biology Mirrors the Divine

The Asher Logic Engine inside Zophiel operates on a core observation:

> **Technology = Biology = Divine Pattern**

Every piece of human technology was reverse-engineered from something in nature, which itself mirrors deeper universal law. Zophiel encodes this as equation chains:

```
┌──────────────────────────────────────────────────────────────────────┐
│                     BIOMIMICRY EQUATION CHAINS                       │
│                                                                      │
│  neural network  =  based off  =  brain neurons                      │
│                  =  based off  =  biological signal routing          │
│                                                                      │
│  database        =  based off  =  brains                             │
│                  =  based off  =  neural storage                     │
│                                                                      │
│  internet        =  based off  =  mycelium networks                  │
│                  =  based off  =  distributed biological comms       │
│                                                                      │
│  algorithm       =  based off  =  decision trees                     │
│                  =  based off  =  human reasoning patterns           │
│                                                                      │
│  sonar           =  based off  =  bat echolocation                   │
│                  =  based off  =  sound-wave physics                 │
│                                                                      │
│  solar cell      =  based off  =  photosynthesis                     │
│                  =  based off  =  light-to-energy conversion         │
└──────────────────────────────────────────────────────────────────────┘
```

The Asher engine applies this same 3-layer decode to any concept — stripping surface appearance to expose mechanism, then truth:

```
SURFACE   →  what it looks like it does
MECHANISM →  what it is actually built to do
TRUTH     →  what it reveals about the deeper pattern
```

---

## ◈ The Zophiel Pipeline — Full Architecture

```
                        ┌─────────────────┐
                        │   USER  QUERY   │
                        └────────┬────────┘
                                 │
                    ─────────────▼─────────────
                   │                           │
           ┌───────▼────────┐         ┌────────▼────────┐
           │   FAST PATH    │   YES   │  Math / Constant │
           │  math / const  ├────────►│    Instant ans.  │
           └───────┬────────┘         └─────────────────┘
                   │ NO
                   ▼
           ┌───────────────┐
           │  CYBER DEFENCE│  ← STRIDE threat analysis
           │    OVERLAY    │    PQC signature check
           └───────┬───────┘    Sovereign Organism guard
                   │
                   ▼
           ┌───────────────┐
           │  ZOPHIEL LIVE │  ← DuckDuckGo live search
           │    SEARCH     │    (if AUREON_WEB_SEARCH=1)
           └───────┬───────┘
                   │
                   ▼
           ┌───────────────────────────────────────┐
           │            TF-IDF RAG ENGINE           │
           │                                        │
           │  Query → tokenize → term frequency     │
           │        → IDF weighting → cosine sim    │
           │        → top-K document retrieval      │
           │                                        │
           │  Corpus: 29,741 docs · 54 domains      │
           │  Storage: SQLite (aureon.db)            │
           └───────────────┬───────────────────────┘
                           │
                           ▼
           ┌───────────────────────────────────────┐
           │          ASHER LOGIC DECODE            │
           │                                        │
           │  Layer 1 — Surface   (what it seems)   │
           │  Layer 2 — Mechanism (what it does)    │
           │  Layer 3 — Truth     (what it means)   │
           │                                        │
           │  + Biomimicry equation chains          │
           │  + Control mechanism decode            │
           │  + Axiom pattern matching              │
           └───────────────┬───────────────────────┘
                           │
                           ▼
           ┌───────────────────────────────────────┐
           │        HUMANLIKE SYNTHESIZER           │
           │                                        │
           │  Merges RAG hits + Asher decode        │
           │  → natural voice response              │
           │  → no hallucination (corpus-only)      │
           └───────────────┬───────────────────────┘
                           │
                           ▼
                  ┌─────────────────┐
                  │    RESPONSE     │
                  │  reply + method │
                  │  + cyber + hits │
                  └─────────────────┘
```

---

## ◈ Brain Architecture — 90+ Modules

```
brain/
│
├── cortex.py              ← Central orchestrator. Routes queries.
│
├── mind/                  ← 22 cognitive modules
│   ├── asher_logic_engine      ← 3-layer decode + equation chains
│   ├── intuition_fast_path     ← Instant answers (math, constants)
│   ├── humanlike_synthesizer   ← Natural language output
│   ├── logic_engine            ← Formal reasoning
│   ├── pattern_detector        ← Recurring patterns across domains
│   ├── working_memory          ← Short-term context retention
│   ├── theory_of_mind          ← Modeling user intent
│   ├── analogy_engine          ← Cross-domain analogies
│   ├── truth_filter            ← Fact vs. noise separation
│   ├── temporal_reasoning      ← Time-aware inference
│   ├── query_expander          ← Semantic query broadening
│   ├── confidence_calibration  ← Certainty scoring
│   └── ...10 more modules
│
├── regions/               ← 6 brain regions (collect / evaluate / reward ...)
│
├── domains/               ← 54-domain knowledge taxonomy
│   └── knowledge_taxonomy.json
│
├── vector_rag.py          ← TF-IDF retrieval over SQLite
├── pgvector_store.py      ← Vector embedding store
└── web_search.py          ← Live DuckDuckGo integration
```

---

## ◈ TF-IDF RAG — How the Retrieval Works

Zophiel does not use a neural embedding model. It uses **TF-IDF** (Term Frequency — Inverse Document Frequency), a deterministic mathematical method:

```
┌──────────────────────────────────────────────────────────┐
│                   TF-IDF EXPLAINED                        │
│                                                           │
│  TF(t, d)  =  how often term t appears in document d     │
│                                                           │
│  IDF(t)    =  log( N / df(t) )                           │
│               N   = total documents                       │
│               df  = documents containing term t           │
│                                                           │
│  Score(t,d) = TF × IDF                                   │
│                                                           │
│  High score = term is common in THIS doc but rare overall │
│             = the term is distinctive and meaningful      │
│                                                           │
│  Query → score all docs → rank → return top K            │
└──────────────────────────────────────────────────────────┘
```

**Why not embeddings?**  
Embeddings (like those in GPT) require a neural model that can "hallucinate" relevance. TF-IDF is mathematically exact — if the term is not in the corpus, it will not fabricate a match.

---

## ◈ Cyber Defence Layer — Nomad Algorithm

The `cyber/` module wraps every query through a post-quantum cryptographic defence stack:

```
┌─────────────────────────────────────────────────────────┐
│               NOMAD CYBER DEFENCE STACK                  │
│                                                          │
│  STRIDE Threat Model                                     │
│    Spoofing · Tampering · Repudiation                    │
│    Info Disclosure · Denial of Service · Elevation      │
│                                                          │
│  Post-Quantum Cryptography (PQC)                         │
│    Kyber-1024  — Key encapsulation (NIST PQC Level 5)   │
│    Dilithium-5 — Digital signatures (NIST PQC Level 5)  │
│                                                          │
│  Imperial Cipher Stack                                   │
│    Scytale → Augustan → Egyptian → Persian Seal         │
│    (historical → modern cipher chain)                    │
│                                                          │
│  Sovereign Organism                                      │
│    Self-healing · Vital guard · Organ registry           │
└─────────────────────────────────────────────────────────┘
```

### ◈ The Sovereign Organism as a Living Immune System

The cyber layer doesn't mimic one animal — it mimics a **living body's immune
system**. Defence is split into innate (always-on barriers) and adaptive
(memory of past attackers), exactly like real biology:

```
┌──────────────────────────────────────────────────────────────────────┐
│                    INNATE IMMUNITY (barriers)                         │
│  Gateway Skin · Rate Nerves · SSRF Lungs · body-size barrier         │
│  Toll-like receptors → signature-scan SQLi/XSS/traversal/cmd-inject  │
│  (recognise the SHAPE of an attack without having seen it before)     │
├──────────────────────────────────────────────────────────────────────┤
│                  ADAPTIVE IMMUNITY (memory)                           │
│  Antibody Memory  → per-client threat "titer" rises on each offense  │
│                     and DECAYS exponentially (immune memory fades)    │
│  Quarantine       → high titer isolates a client; auto-released       │
│  Secondary resp.  → repeat offenders escalate FASTER (memory boost)   │
│  Inflammation     → graded body-wide fever clamps throughput under    │
│                     sustained assault, then heals to baseline         │
│  Vaccination      → pre-seed immunity from an IOC feed (ZOPHIEL_IOC_  │
│                     FILE) so known-bad clients are blocked on contact │
│  Persistence      → antibody memory saved to disk — survives restart  │
└──────────────────────────────────────────────────────────────────────┘
```

The body has **17 interlocking organs** in a dependency graph. A child organ
checks its parents on every pulse — so a single critical organ trips a
body-wide lockdown, and the interlocking chains mean an attacker must keep
*all* organs healthy to operate. Live state is exposed at `GET /organism/vitals`.

Tests: `python test_immune_system.py` (9 tests — titer/decay/quarantine,
secondary response, fever+recovery, 17-organ lockdown, toll-like receptors,
persistence, vaccination, live-gate integration).

---

## ◈ What Makes This Different From an LLM

```
┌──────────────────────┬─────────────────────────────────────────────┐
│ Property             │ LLM (GPT, Claude, Gemini)  │ Zophiel/SOLIA  │
├──────────────────────┼────────────────────────────┼────────────────┤
│ Architecture         │ Transformer + attention     │ TF-IDF + rules │
│ Training             │ 100B+ internet tokens       │ 29,741 curated │
│ Hallucination        │ Common                      │ None (by design│
│ Reasoning            │ Statistical completion      │ Equation chains│
│ Explainability       │ Black box                   │ Full trace     │
│ PQC Security         │ None                        │ Kyber+Dilithium│
│ Philosophy layer     │ None                        │ Asher decode   │
│ Corpus control       │ None (trained on web)       │ Full (yours)   │
│ Cost to run          │ $$$ GPU clusters            │ Single server  │
└──────────────────────┴────────────────────────────┴────────────────┘
```

---

## ◈ Quick Start

```bash
# Install
pip install -r requirements.txt

# Run
python app.py

# Query
curl -X POST http://localhost:8080/ask \
  -H "Content-Type: application/json" \
  -d '{"query": "How does quantum entanglement work?"}'

# Health check
curl http://localhost:8080/health
```

### Enable Live Web Search

```bash
AUREON_WEB_SEARCH=1 python app.py
```

---

## ◈ API Reference

| Endpoint | Method | Body | Returns |
|----------|--------|------|---------|
| `/ask` | `POST` | `{"query": "..."}` | `{reply, method, hits, cyber, asher}` |
| `/health` | `GET` | — | `{status, docs}` |
| `/` | `GET` | — | Engine info |

### Response Shape

```json
{
  "reply":  "The answer, synthesized from corpus + logic",
  "method": "rag+asher_decode+synthesize",
  "hits":   8,
  "cyber":  null,
  "asher":  { "surface": "...", "mechanism": "...", "truth": "..." }
}
```

---

## ◈ Deploy to Railway

```
1.  Push this repo to GitHub
2.  Railway → New Project → Deploy from GitHub → select repo
3.  Railway auto-detects railway.toml → runs python app.py
4.  Optional: set AUREON_WEB_SEARCH=1 in Railway env vars
5.  Live at: https://your-app.railway.app
```

---

## ◈ Run Full Test Suite

```bash
python full_domain_test.py
# Expected: 60/60 questions synthesized across 6 domains

python aureon_test_runner.py
# Runs complete pipeline validation
```

---

## ◈ Project Structure

```
Zophiel/
├── app.py                     ← Flask API entry point
├── aureon_test_runner.py      ← Full pipeline test harness
├── cyber_defence.py           ← STRIDE + PQC threat analysis
├── full_domain_test.py        ← 60-question domain coverage test
├── generate_real_corpus.py    ← Corpus generation scripts
├── requirements.txt
├── railway.toml
│
├── brain/                     ← 90+ intelligence modules
│   ├── cortex.py
│   ├── mind/                  ← 22 cognitive modules
│   ├── regions/               ← 6 brain regions
│   ├── domains/               ← 54-domain taxonomy
│   └── ...
│
├── cyber/                     ← Nomad Cyber Algorithm (TypeScript)
│   └── nomad_cyber_algorithm-main/
│       └── src/               ← Kyber1024, Dilithium5, Imperial stack
│
└── data/
    ├── aureon.db              ← SQLite corpus (rebuilt fresh on each deploy)
    ├── corpus_knowledge.json  ← 55-domain knowledge base (inc. Cybersecurity)
    └── aureon_full_dataset_repaired.json
```

---

## ◈ What's New

### v1.2.0 — June 2026

| Feature | Detail |
|---|---|
| **API Key Auth** | `POST /ask` protected by `Authorization: Bearer <key>`. Set `ZOPHIEL_API_KEY` env var on Railway. `/health` and `/` remain public. |
| **Live Internet Search** | Auto-triggers Wikipedia fallback when corpus confidence < 0.35. Google News RSS for current-events queries. No API key needed. |
| **Code Generation Engine** | 26 verified patterns: data structures, sorting, search, graph, DP, strings, SQL, OOP, functional, async, security, concurrency. Every pattern has type hints, Big-O docs, and passing assert tests. |
| **Opinion & Decision Mode** | Zophiel now forms a position when asked to decide. Trigger phrases like "you have to decide", "I need you to help me", "what's your take". Covers 12 ethical domains: death penalty, abortion, war, climate, wealth inequality, God, AI rights, lying, drugs, and more. |
| **Ethical Reasoning** | 3-layer Asher decode applied to moral questions: Surface → Mechanism → Truth. Positions are data-driven and logically traced, not sentiment-based. |
| **Railway Startup Fix** | Flask binds immediately on startup; corpus builds in background thread. Healthcheck passes in <1s instead of timing out. |
| **Background Boot** | Corpus always rebuilt from JSON on deploy — no stale/malformed DB on Railway. |

### Connecting Zophiel to Your Frontend (Aureon App)

**Recommended scope: Option A — Chat-only fallback now, expand later.**

Zophiel is a single-turn Q&A engine. It does not support streaming, multi-turn history, system prompts, vision, or JSON schema output. Route only simple chat queries to `/ask`. Leave Search, Briefings, Consensus, and Vision on your existing Gemini/BYOK path.

```
Frontend request:
  POST https://zophielalgorithm-production.up.railway.app/ask
  Authorization: Bearer <ZOPHIEL_API_KEY>
  Content-Type: application/json
  { "query": "your question here" }

Response:
  {
    "reply": "...",
    "method": "rag | fast_path | code_engine | web_fallback(wikipedia) | live_search",
    "confidence": 0.87,
    "web_hits": 0,
    "asher": "..."
  }
```

**Railway env var to set:**
```
ZOPHIEL_API_KEY=your-secret-key-here
```

**Frontend header to send:**
```
Authorization: Bearer your-secret-key-here
```

---

### Pipeline Decision Tree

```
Query
  |
  +-- Math/constant? ──────────────────> fast_path (0ms)
  |
  +-- Coding request? ─────────────────> code_engine (26 patterns, 0ms)
  |
  +-- "You have to decide" / opinion? -> opinion_engine (12 ethical domains)
  |
  +-- Identity / self-reflection? ────> identity (sovereign Zophiel response)
  |
  +-- News / "today" / "2026"? ───────> Google News RSS (live headlines)
  |
  +-- Corpus confidence >= 0.35? ─────> TF-IDF RAG (55-domain corpus)
  |
  +-- Corpus confidence < 0.35? ──────> Wikipedia fallback (live web data)
```

---

<div align="center">

**Built by Aureon Software**  
*Zophiel / SOLIA — Deterministic Intelligence. No hallucination. Full trace.*

</div>
